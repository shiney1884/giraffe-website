// const jsdom = require('jsdom');
// const JSDOM = jsdom.JSDOM;

// let products = JSDOM.window.document.querySelectorAll('.product a');


function click(e) {
    e.preventDefault();
    // let id = e.path[1].id;
    // let url = e.path[0].pathname;
    console.log(e);
}